import React from 'react';
import { MdSchedule } from 'react-icons/md';

export const HeaderLeft = (): React.ReactElement => {
  return (
    <div className="header-left">
      <MdSchedule size={25} />
    </div>
  );
};
